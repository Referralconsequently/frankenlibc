/target/
target-*/
legacy_glibc_code/
*.swp
*.swo
*~
.DS_Store
._*

# Stray image files at repo root (downloads dropped in wrong dir)
/*.jpeg
/*.jpg
/*.png

# bv (beads viewer) local config and caches
.bv/

# Profiling and benchmark data
perf.data
perf.data.*
*.profraw
flamegraph*.svg
scripts/perf_baseline.json

# Local scratch DBs
storage.sqlite3
storage.sqlite3-*

# Build artifacts
a.out

# Beads history snapshots
.beads/.br_history/

# bv graph output
--graph-format.svg

# Python cache
__pycache__/
*.pyc

# Temporary directories
.tmp/

# Symlinked target directories
target_sym*/

# FastEmbed model caches (downloaded at runtime)
.fastembed_cache/

# Agent coordination config
.agent-mail.yaml

# Compiler crash reports
rustc-ice-*.txt

# Ephemeral agent flag/lock files
Safe

# Ephemeral files (agent-generated, build artifacts, test detritus)
ci-artifacts/
proptest-regressions/
test_meminfo

# Agent session ephemeral files
SESSION_TODO.md
SESSION_REPORT.md
SESSION_*.md
verify_*.sh
newfile.txt
repro_*.rs

# Nested crate build artifacts
crates/*/target/

# Codex/agent build artifacts
target-codex*/
target_*/

# Claude local settings
.claude/settings.local.json

# Agent mail project identity
.agent-mail-project-id

# SQLite backup/corruption/recovery files
*.sqlite3.bak
*.sqlite3-wal.bak
*.sqlite3.corrupt-*
*.sqlite3.corrupt_*
*.sqlite3.pre_recovery_*
storage.recover*.sql
storage.recovered.sqlite3

# Beads backup/recovery files (agent-generated snapshots)
.beads/*.bak.*
.beads/*.corrupt_*
.beads/*.pre_recovery_*
.beads/*.recover*.sql
.beads/*.orphan_*

# Ephemeral scratch files
baseline_decisions

# System trace/debug logs
strace.log
strace*.log
ltrace*.log

# Remote compilation helper artifacts
.rch-target/
.rch-deploy-root/
rch-bin/
rch-install/
.env

# Core dumps and agent config backups
core.*
core
*.mcp.json.*.bak
cline.mcp.json
opencode.json
windsurf.mcp.json
clippy.txt
clippy.json
clippy_results.json
clippy_unwrap.json
clippy_unwrap_clean.txt
check_output.txt
.next_backup*/
storage.sqlite3
storage.sqlite3-shm
storage.sqlite3-wal
storage.sqlite3.bak
storage.sqlite3.corrupt-*
storage.sqlite3-shm.corrupt-*
storage.sqlite3-wal.corrupt-*
